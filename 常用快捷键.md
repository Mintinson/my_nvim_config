### 不退出 neovim 快速重新加载配置

" 修改配置后
`<leader>rr`       " 重载配置 （见 keymapping）
`:Lazy sync`        " 如果改了插件配置，同步插件
`:checkhealth`      " 检查配置是否正常

### 在Visual模式下替换选中文本中的词

命令: :`s/old/new/g`

常用标志位：
* g - 替换行内所有匹配
* c - 每次替换前确认
* i - 忽略大小写

### 对同一个buffer进行分屏

命令:
* :split 或 :sp - 水平分屏（上下）
* :vsplit 或 :vsp - 垂直分屏（左右）
* <C-w>s - 水平分屏快捷键
* <C-w>v - 垂直分屏快捷键
解释: 这些命令会将当前窗口分割为两个，显示同一个 buffer，可以在不同位置查看/编辑同一文件

常用窗口操作:

* <C-w>q - 关闭当前窗口
* <C-w>o - 只保留当前窗口（关闭其他）
* <C-w>= - 平均分配所有窗口大小
* <C-w>_ - 最大化当前窗口高度
* <C-w>| - 最大化当前窗口宽度

### `nvim-tree` 中不同的显示文件选项

在 `nvim-tree` 界面中

| 快捷键 | 功能                  | 说明          |
| --- | ------------------- | ----------- |
| `I` | 切换显示 git ignored 文件 | 在文件树窗口按 `I` |
| `H` | 切换显示隐藏文件（dotfiles）  | 在文件树窗口按 `H` |
| `U` | 切换显示未追踪文件           | 在文件树窗口按 `U` |


### `vim` 原生搜索进阶

#### 基础搜索流程（类似 VSCode Ctrl+F）：

1. 按 `/` 开始搜索（或 `?` 反向搜索）
2. 输入搜索词，回车
3. 按 `n` 跳到下一个，`N` 跳到上一个
4. 按 `\\` 清除高亮

#### 大小写控制选项：

- **默认行为（智能大小写）:**
    
    - `/hello` → 匹配 `hello`, `Hello`, `HELLO`
    - `/Hello` → 只匹配 `Hello`（因为包含大写）
- **强制区分大小写:** `/\CHello`
- **强制忽略大小写:** `/\chello`
    
#### 完整单词匹配：

```
/\<function\>   " 只匹配完整的 "function"，不匹配 "functions" 或 "myfunction"
```
#### 高级搜索技巧：

```
/word1\|word2        " 搜索 word1 或 word2
/\vword1|word2       " 同上（very magic 模式）
/\<\w\{4}\>          " 搜索 4 个字母的单词
/^function           " 搜索行首的 function
/function$           " 搜索行尾的 function
```
#### 当前光标下的单词：

- `*` - 向下搜索当前光标下的完整单词
- `#` - 向上搜索当前光标下的完整单词
- `g*` - 向下搜索（部分匹配）
- `g#` - 向上搜索（部分匹配）

### `vim` 原生匹配跳转

- **命令:**
    - `%` - 跳转到匹配的括号（`{}`, `[]`, `()` 等）
    - `[{` - 跳转到当前代码块的开始 `{`
    - `]}` - 跳转到当前代码块的结束 `}`
    - `[(` - 跳转到当前括号块的开始 `(`
    - `])` - 跳转到当前括号块的结束 `)`
    - `[m` - 跳转到上一个方法/函数的开始
    - `]m` - 跳转到下一个方法/函数的开始

### 给一段文本添加包围符号（比如 "", <>, ... ）

见 `edit.lua` 中的 `mini.surround` 插件

例子：
* 添加引号： `gsaiw"`
* 删除引号： `gsd”`
* 将双引号替换为单引号： `gsr"`